{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Fastapi",
            "type": "python",
            "request": "attach",
            "port": 5858,
            "host": "localhost",
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}/app",
                    "remoteRoot": "/app"
                }
            ],
        }
    ]
}